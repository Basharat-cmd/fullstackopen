sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST /new_note (via JavaScript fetch)
    activate server
    Note right of server: Server saves the new note
    server-->>browser: JSON response with the new note
    deactivate server

    Note right of browser: JavaScript updates the page to show the new note
